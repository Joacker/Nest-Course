
export let name = 'Fernando';
export const age: Number = 35;
export const isValid: Boolean = true;

name = 'Melisa';

export const templateString = `Hola ${name}, tienes ${age} años y tu validez es ${isValid ? 'válida' : 'inválida'}.
Puede tener " dobles
' simple`;
